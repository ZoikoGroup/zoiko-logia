# Stack per ZoikoLogia_Project_Execution_Roadmap.docx "Recommended tech stack" table.

fastapi
uvicorn[standard]

sqlalchemy
alembic
aiosqlite
greenlet
asyncpg
psycopg2-binary

python-multipart
email-validator
pypdf
python-docx
openpyxl
python-pptx
reportlab
Pillow

# Supabase Auth — JWKS token verification + Admin API calls
pyjwt[crypto]
httpx

pydantic
pydantic-settings

# ML Classification
transformers
torch

# LLM providers
openai
groq
google-genai

# Rate limiting
slowapi
redis
celery

# Test runner for backend/tests. pytest-asyncio is required because the
# tenant-isolation and licence-gate suites are written as bare `async def
# test_*` with no marker — pytest.ini sets asyncio_mode = auto so they run
# under pytest as well as standalone via their own main().
pytest
pytest-asyncio
