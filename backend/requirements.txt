# Stack per ZoikoLogia_Project_Execution_Roadmap.docx "Recommended tech stack" table.
# This slice (Login only) uses SQLite for zero-friction local dev; Postgres+pgvector
# gets introduced when Source Library/RAG actually needs vector search.

fastapi
uvicorn[standard]

sqlalchemy
aiosqlite
greenlet

python-jose[cryptography]
bcrypt
python-multipart
email-validator

pydantic
pydantic-settings
